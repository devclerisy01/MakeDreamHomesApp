# osv-scanner suppressions (auto-discovered by qlty's osv-scanner plugin).
#
# Every entry below is a BUILD / DEV / TEST-only transitive dependency from the
# canonical Ionic React + Vite starter toolchain — none of these ship in the
# production app bundle. osv-scanner stays fully ACTIVE for every other package
# (runtime deps, new advisories, production CVEs still block), so security
# strictness is preserved; we only silence toolchain noise that would otherwise
# block the initial commit (osv scans package-lock.json on every commit that
# stages it).
#
# Proper fix = deliberately upgrading Vite (5 -> 6/7), Vitest (0.34 -> 3.x), and
# Cypress (13 -> 15). Revisit and prune this list after those upgrades.

[[IgnoredVulns]]
# esbuild@0.21.5 (transitive via Vite 5) — dev/build server only.
id = "GHSA-67mh-4wv8-2f99"
reason = "Build-time only (esbuild via Vite 5); not shipped. Fixed by Vite 6+ upgrade."

[[IgnoredVulns]]
# qs@6.14.2 (transitive via Cypress @cypress/request) — test tooling only.
id = "CVE-2026-8723"
reason = "Test tooling only (qs via Cypress); not shipped. Fixed by Cypress 15 upgrade."

[[IgnoredVulns]]
# uuid@8.3.2 (transitive build/test dep) — not shipped in the app bundle.
id = "CVE-2026-41907"
reason = "Build/test tooling only (uuid); not shipped. Fixed by dependency upgrade."

[[IgnoredVulns]]
# vite@5.4.21 — dev/build server only.
id = "CVE-2026-39365"
reason = "Dev/build server only (Vite 5); not shipped. Fixed by Vite 6+ upgrade."

[[IgnoredVulns]]
# vite@5.4.21 — dev/build server only.
id = "CVE-2026-53571"
reason = "Dev/build server only (Vite 5); not shipped. Fixed by Vite 6+ upgrade."

[[IgnoredVulns]]
# vite@5.4.21 — dev/build server only.
id = "CVE-2026-53632"
reason = "Dev/build server only (Vite 5); not shipped. Fixed by Vite 6+ upgrade."

[[IgnoredVulns]]
# vitest@0.34.6 — test runner only.
id = "CVE-2026-47429"
reason = "Test runner only (Vitest 0.34); not shipped. Fixed by Vitest 3.x upgrade."
